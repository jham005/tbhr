import React from 'react';
import Layout from '../components/layout';

const NotFoundPage = () => (
  <Layout>
    <h1>You are lost!</h1>
    <p>It happens.</p>
  </Layout>
);

export default NotFoundPage;
